# Example environment variables
PORT=3000
NODE_ENV=development
# Add other variables as needed (API keys, RPC endpoints, etc.)
# Allowed origins for CORS (comma separated). Example for local development:
# ALLOWED_ORIGINS=http://localhost:3000

